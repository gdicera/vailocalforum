<!DOCTYPE html>
<html>
  <head>
    <title>VaiLocal Forum</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootsgrap.bundle.min.js"></script>


    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.css">

    <link rel="stylesheet" href = "/stylesheets/application.css">

  <style>
  @import url('https://fonts.googleapis.com/css2?family=Yeseva+One&display=swap');
  </style>

  </head>

  <body>

  <nav class="v-font navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">VaiLocal</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/topics/">Topics</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/posts/">Posts</a>
          </li>

          <% if @current_user == nil %>
          <li class="nav-item"><a class="nav-link" href="/user_sign_in">Sign In</a></li>
          <li class="nav-item"><a class="nav-link" href="/">Sign Up</a></li>

          <% else %>
          <li class="nav-item"><a class="nav-link" href="/user_sign_out/">Sign Out</a></li>
          
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

    <% if notice.present? %>
    <div style = "border: thin green solid; color: green" class="v-font">
      <%= notice %></div>
    <% end %>

    <% if alert.present? %>
    <div style = "border: thin red solid; color: red" class="v-font"><%= alert %></div>
    <% end %>
    
    <%= yield %>
  </body>
</html>
