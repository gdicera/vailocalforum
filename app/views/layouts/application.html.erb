<!DOCTYPE html>
<html>
  <head>
    <title>VaiLocal Forum</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootsgrap.bundle.min.js"></script>


    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.css">

    <link rel="stylesheet" href = "/stylesheets/application.css">


  </head>

  <body>
    <ul>
      <h1 class="text-center">VaiLocal</h1>

      <% if @current_user == nil %>
      <li><a href = "/user_sign_in" class = "btn btn-outline-primary">Sign In</a></li>
      <li><a href = "/" class = "btn btn-outline-primary">Sign Up</a></li>

      <% else %>
      <li><a href = "/topics" class = "btn btn-outline-primary">Topics</a></li>
      <li><a href = "/user_sign_out" class = "btn btn-outline-primary">Sign Out</a></li>

      <% end %>
    </ul>

    <% if notice.present? %>
    <div style = "border: thin green solid; color: green">
      <%= notice %></div>
    <% end %>

    <% if alert.present? %>
    <div style = "border: thin red solid; color: red"><%= alert %></div>
    <% end %>
    
    <%= yield %>
  </body>
</html>
